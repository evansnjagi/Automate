# Automate boring task with Python

## Questions

Write one line that takes the string `"  John Doe, 30  "`, strip whitespace, split on comma, and print `"Name: John Doe, Age: 30"` using an f-string

Solution:

```bash
python q1.py
```

Given files = ["a.jpg", "b.txt", "c.csv", "d.jpg", "e.png"], write a list comprehension that returns just the filenames ending in .jpg or .png, converted to uppercase.

Solution:

```python

```